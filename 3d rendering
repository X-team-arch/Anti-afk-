local Rayfield = loadstring(game:HttpGet("https://sirius.menu/gen2"))()

local RunService = game:GetService("RunService")
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local VirtualUser = game:GetService("VirtualUser")
local UserInputService = game:GetService("UserInputService")
local Lighting = game:GetService("Lighting")
local Workspace = game:GetService("Workspace")
local Camera = Workspace.CurrentCamera

-- ==========================================
-- 🛡️ PROTECCIÓN ANTI-KICK & BYPASS
-- ==========================================
if getrawmetatable and setreadonly then
    local gmt = getrawmetatable(game)
    setreadonly(gmt, false)
    local oldNamecall = gmt.__namecall

    gmt.__namecall = newcclosure(function(self, ...)
        local method = getnamecallmethod()
        if method == "Kick" or method == "kick" then
            return nil
        end
        return oldNamecall(self, ...)
    end)
    setreadonly(gmt, true)
end

-- ==========================================
-- VENTANA PRINCIPAL (GEN 2)
-- ==========================================
local window = Rayfield:CreateWindow({
    name = "Uʀɪᴇʟ Sᴛᴜᴅɪᴏ",
    subtitle = "Mᴜʟᴛɪ-Gᴀᴍᴇ — Gᴇɴ 2",
    sidebarLayout = true
})

-- Pestañas Generales y Específicas
local sdTab = window:CreateTab({ name = "Sᴀɴ Dɪᴇɢᴏ", icon = "car" })
local buildTab = window:CreateTab({ name = "Tᴇʀᴍɪ́ɴᴀʟᴏ!", icon = "hammer" })
local combatTab = window:CreateTab({ name = "Cᴏᴍʙᴀᴛᴇ", icon = "crosshair" })
local vehicleTab = window:CreateTab({ name = "Vᴇʜɪ́ᴄᴜʟᴏs", icon = "gauge" })
local movementTab = window:CreateTab({ name = "Mᴏᴠɪᴍɪᴇɴᴛᴏ", icon = "user" })
local espTab = window:CreateTab({ name = "Vɪsɪᴏ́ɴ", icon = "eye" })
local tpTab = window:CreateTab({ name = "Tᴇʟᴇᴘᴏʀᴛs", icon = "map-pin" })
local afkTab = window:CreateTab({ name = "AFK / GPU", icon = "zap" })

-- Variables de Estado
local antiCollisionConn, borderBypassConn, antiPitConn, infiniteGasConn
local autoPlasterConn, autoBricksConn, noclipConn, infJumpConn, antiAfkConn, carFlyConn, noRecoilConn
local autoPlasterEnabled = false
local autoBricksEnabled = false
local workRange = 25
local carFlying = false
local carFlySpeed = 100

-- Variables Aimbot
local aimbotEnabled = false
local aimbotTargetPart = "Head"
local aimbotFovRadius = 150
local aimbotSmoothness = 0.3
local showFovCircle = false
local aimbotConn, fovCircle

if Drawing then
    fovCircle = Drawing.new("Circle")
    fovCircle.Thickness = 1.5
    fovCircle.Color = Color3.fromRGB(0, 255, 200)
    fovCircle.Filled = false
    fovCircle.Transparency = 0.8
    fovCircle.Visible = false
end

local function getMyVehicle()
    local char = LocalPlayer.Character
    if char and char:FindFirstChildOfClass("Humanoid") then
        local seat = char:FindFirstChildOfClass("Humanoid").SeatPart
        if seat and seat.Parent then
            return seat.Parent, seat
        end
    end
    return nil, nil
end

local function getClosestPlayerToCursor()
    local closestPlayer = nil
    local shortestDistance = aimbotFovRadius
    local mousePos = UserInputService:GetMouseLocation()

    for _, player in pairs(Players:GetPlayers()) do
        if player ~= LocalPlayer and player.Character and player.Character:FindFirstChild(aimbotTargetPart) then
            local hum = player.Character:FindFirstChildOfClass("Humanoid")
            if hum and hum.Health > 0 then
                local targetPart = player.Character[aimbotTargetPart]
                local screenPos, onScreen = Camera:WorldToViewportPoint(targetPart.Position)

                if onScreen then
                    local dist = (Vector2.new(screenPos.X, screenPos.Y) - mousePos).Magnitude
                    if dist < shortestDistance then
                        shortestDistance = dist
                        closestPlayer = player
                    end
                end
            end
        end
    end
    return closestPlayer
end

-- ==========================================
-- 🇲🇽 1. SAN DIEGO BORDER
-- ==========================================
sdTab:CreateToggle({
    name = "Aɴᴛɪᴄᴏʟɪsɪᴏ́ɴ ᴅᴇ Vᴇʜɪ́ᴄᴜʟᴏs",
    flag = "SD_AntiCollision",
    value = false,
    callback = function(value)
        if value then
            antiCollisionConn = RunService.Stepped:Connect(function()
                local veh = getMyVehicle()
                if veh then
                    for _, part in pairs(veh:GetDescendants()) do
                        if part:IsA("BasePart") then part.CanCollide = false end
                    end
                end
            end)
        else
            if antiCollisionConn then antiCollisionConn:Disconnect() antiCollisionConn = nil end
        end
    end
})

sdTab:CreateToggle({
    name = "Bʏᴘᴀss Fʀᴏɴᴛᴇʀᴀ (Pᴀsᴀ Mᴜʀᴏs/Pᴇᴀᴊᴇs)",
    flag = "SD_NoBorderLimit",
    value = false,
    callback = function(value)
        if value then
            borderBypassConn = RunService.Stepped:Connect(function()
                for _, obj in pairs(Workspace:GetDescendants()) do
                    if obj:IsA("BasePart") and (obj.Name:lower():find("border") or obj.Name:lower():find("gate") or obj.Name:lower():find("wall") or obj.Name:lower():find("barrera")) then
                        obj.CanCollide = false
                        obj.CanTouch = false
                    end
                end
            end)
        else
            if borderBypassConn then borderBypassConn:Disconnect() borderBypassConn = nil end
        end
    end
})

sdTab:CreateToggle({
    name = "Aɴᴛɪ PIT / Aɴᴛɪ-Vᴏʟᴄᴀᴅᴜʀᴀ",
    flag = "SD_AntiPIT",
    value = false,
    callback = function(value)
        if value then
            antiPitConn = RunService.Heartbeat:Connect(function()
                local veh, seat = getMyVehicle()
                if veh and seat then
                    local root = veh.PrimaryPart or seat
                    root.AssemblyAngularVelocity = Vector3.new(0, root.AssemblyAngularVelocity.Y, 0)
                end
            end)
        else
            if antiPitConn then antiPitConn:Disconnect() antiPitConn = nil end
        end
    end
})

sdTab:CreateToggle({
    name = "Gᴀsᴏʟɪɴᴀ Iɴғɪɴɪᴛᴀ",
    flag = "SD_InfGas",
    value = false,
    callback = function(value)
        if value then
            infiniteGasConn = RunService.Heartbeat:Connect(function()
                local veh = getMyVehicle()
                if veh then
                    for _, v in pairs(veh:GetDescendants()) do
                        if v:IsA("NumberValue") or v:IsA("IntValue") then
                            if v.Name:lower():find("gas") or v.Name:lower():find("fuel") then
                                v.Value = 100
                            end
                        end
                    end
                end
            end)
        else
            if infiniteGasConn then infiniteGasConn:Disconnect() infiniteGasConn = nil end
        end
    end
})

-- ==========================================
-- 🛠️ 2. TERMÍNALO!
-- ==========================================
buildTab:CreateToggle({
    name = "Aᴜᴛᴏ-Aᴘʟɪᴄᴀᴅᴏʀ / Pɪɴᴛᴀᴅᴏ Rᴀ́ᴘɪᴅᴏ",
    flag = "AutoPlaster",
    value = false,
    callback = function(value)
        autoPlasterEnabled = value
        if autoPlasterEnabled then
            autoPlasterConn = RunService.Heartbeat:Connect(function()
                local char = LocalPlayer.Character
                if char and char:FindFirstChild("HumanoidRootPart") then
                    local hrpPos = char.HumanoidRootPart.Position
                    for _, obj in pairs(Workspace:GetDescendants()) do
                        if obj:IsA("BasePart") and (obj.Name:lower():find("wall") or obj.Name:lower():find("plaster") or obj.Name:lower():find("paint")) then
                            if (obj.Position - hrpPos).Magnitude <= workRange then
                                if obj:FindFirstChildOfClass("ClickDetector") then
                                    fireclickdetector(obj:FindFirstChildOfClass("ClickDetector"))
                                end
                            end
                        end
                    end
                end
            end)
        else
            if autoPlasterConn then autoPlasterConn:Disconnect() autoPlasterConn = nil end
        end
    end
})

buildTab:CreateToggle({
    name = "Aᴜᴛᴏ-Aᴄᴏᴍᴏᴅᴀᴅᴏʀ ᴅᴇ Lᴀᴅʀɪʟʟᴏs",
    flag = "AutoBricks",
    value = false,
    callback = function(value)
        autoBricksEnabled = value
        if autoBricksEnabled then
            autoBricksConn = RunService.Heartbeat:Connect(function()
                local char = LocalPlayer.Character
                if char and char:FindFirstChild("HumanoidRootPart") then
                    local hrpPos = char.HumanoidRootPart.Position
                    for _, brick in pairs(Workspace:GetDescendants()) do
                        if brick:IsA("BasePart") and (brick.Name:lower():find("brick") or brick.Name:lower():find("block")) then
                            if (brick.Position - hrpPos).Magnitude <= workRange then
                                if brick:FindFirstChildOfClass("ClickDetector") then
                                    fireclickdetector(brick:FindFirstChildOfClass("ClickDetector"))
                                end
                            end
                        end
                    end
                end
            end)
        else
            if autoBricksConn then autoBricksConn:Disconnect() autoBricksConn = nil end
        end
    end
})

buildTab:CreateSlider({
    name = "Rᴀɴɢᴏ ᴅᴇ Aᴄᴄɪᴏ́ɴ (Mᴇᴛʀᴏs)",
    range = {10, 100},
    increment = 5,
    currentValue = 25,
    flag = "WorkRangeVal",
    callback = function(value) workRange = value end,
})

buildTab:CreateButton({
    name = "Tʀᴀᴇʀ Bᴀʟᴅᴇs / Hᴇʀʀᴀᴍɪᴇɴᴛᴀs Cᴇʀᴄᴀɴᴀs",
    callback = function()
        local char = LocalPlayer.Character
        if char and char:FindFirstChild("HumanoidRootPart") then
            local hrp = char.HumanoidRootPart
            for _, item in pairs(Workspace:GetDescendants()) do
                if item:IsA("BasePart") and (item.Name:lower():find("bucket") or item.Name:lower():find("tool") or item.Name:lower():find("spray")) then
                    item.CFrame = hrp.CFrame + Vector3.new(0, 2, 0)
                end
            end
        end
    end
})

-- ==========================================
-- 🎯 3. COMBATE (AIMBOT)
-- ==========================================
combatTab:CreateToggle({
    name = "Aɪᴍʙᴏᴛ Aᴜᴛᴏᴍᴀ́ᴛɪᴄᴏ",
    flag = "AimbotToggle",
    value = false,
    callback = function(value)
        aimbotEnabled = value
        if aimbotEnabled then
            aimbotConn = RunService.RenderStepped:Connect(function()
                if showFovCircle and fovCircle then
                    fovCircle.Position = UserInputService:GetMouseLocation()
                    fovCircle.Radius = aimbotFovRadius
                    fovCircle.Visible = true
                elseif fovCircle then
                    fovCircle.Visible = false
                end

                local targetPlayer = getClosestPlayerToCursor()
                if targetPlayer and targetPlayer.Character and targetPlayer.Character:FindFirstChild(aimbotTargetPart) then
                    local targetPos = targetPlayer.Character[aimbotTargetPart].Position
                    Camera.CFrame = Camera.CFrame:Lerp(CFrame.new(Camera.CFrame.Position, targetPos), aimbotSmoothness)
                end
            end)
        else
            if aimbotConn then aimbotConn:Disconnect() aimbotConn = nil end
            if fovCircle then fovCircle.Visible = false end
        end
    end
})

combatTab:CreateToggle({
    name = "Mᴏsᴛʀᴀʀ Cɪ́ʀᴄᴜʟᴏ FOV",
    flag = "ShowFOV",
    value = false,
    callback = function(value) showFovCircle = value end
})

combatTab:CreateSlider({
    name = "Rᴀᴅɪᴏ ᴅᴇ FOV",
    range = {50, 500},
    increment = 10,
    currentValue = 150,
    flag = "FovRadius",
    callback = function(value) aimbotFovRadius = value end,
})

combatTab:CreateSlider({
    name = "Sᴜᴀᴠɪᴅᴀᴅ (Sᴍᴏᴏᴛʜɴᴇss)",
    range = {1, 10},
    increment = 1,
    currentValue = 3,
    flag = "AimSmooth",
    callback = function(value) aimbotSmoothness = value / 10 end,
})

combatTab:CreateDropdown({
    name = "Pᴀʀᴛᴇ ᴅᴇ Aᴘᴜɴᴛᴀᴅᴏ",
    options = {"Cabeza (Head)", "Torso (Root)"},
    currentOption = {"Cabeza (Head)"},
    flag = "AimPart",
    callback = function(option)
        aimbotTargetPart = (option[1] == "Cabeza (Head)") and "Head" or "HumanoidRootPart"
    end,
})

combatTab:CreateToggle({
    name = "Sɪɴ Rᴇᴛʀᴏᴄᴇsᴏ (Nᴏ Rᴇᴄᴏɪʟ)",
    flag = "NoRecoil",
    value = false,
    callback = function(value)
        if value then
            noRecoilConn = RunService.Stepped:Connect(function()
                if LocalPlayer.Character then
                    local tool = LocalPlayer.Character:FindFirstChildOfClass("Tool")
                    if tool then
                        for _, v in pairs(tool:GetDescendants()) do
                            if v:IsA("Value") and (v.Name:find("Recoil") or v.Name:find("Spread")) then
                                v.Value = 0
                            end
                        end
                    end
                end
            end)
        else
            if noRecoilConn then noRecoilConn:Disconnect() noRecoilConn = nil end
        end
    end
})

-- ==========================================
-- 🏎️ 4. VEHÍCULOS PRO
-- ==========================================
vehicleTab:CreateToggle({
    name = "Vᴜᴇʟᴏ ᴅᴇ Aᴜᴛᴏ (Cᴀʀ Fʟʏ)",
    flag = "CarFly",
    value = false,
    callback = function(value)
        carFlying = value
        if carFlying then
            carFlyConn = RunService.RenderStepped:Connect(function()
                local veh, seat = getMyVehicle()
                if veh and seat then
                    local root = veh.PrimaryPart or seat
                    root.AssemblyLinearVelocity = Camera.CFrame.LookVector * carFlySpeed
                end
            end)
        else
            if carFlyConn then carFlyConn:Disconnect() carFlyConn = nil end
        end
    end
})

vehicleTab:CreateSlider({
    name = "Vᴇʟᴏᴄɪᴅᴀᴅ ᴅᴇ Vᴜᴇʟᴏ Cᴏᴄʜᴇ",
    range = {30, 300},
    increment = 10,
    currentValue = 100,
    flag = "CarFlySpeedVal",
    callback = function(value) carFlySpeed = value end,
})

vehicleTab:CreateButton({
    name = "Fʀᴇɴᴏ Iɴsᴛᴀɴᴛᴀ́ɴᴇᴏ",
    callback = function()
        local veh, seat = getMyVehicle()
        if veh and seat then
            local root = veh.PrimaryPart or seat
            root.AssemblyLinearVelocity = Vector3.zero
            root.AssemblyAngularVelocity = Vector3.zero
        end
    end
})

-- ==========================================
-- 🏃 5. MOVIMIENTO
-- ==========================================
movementTab:CreateSlider({
    name = "Vᴇʟᴏᴄɪᴅᴀᴅ ᴅᴇ Cᴀᴍɪɴᴀᴛᴀ",
    range = {16, 200},
    increment = 2,
    currentValue = 16,
    flag = "WalkSpeedSlider",
    callback = function(value)
        if LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Humanoid") then
            LocalPlayer.Character:FindFirstChildOfClass("Humanoid").WalkSpeed = value
        end
    end,
})

movementTab:CreateToggle({
    name = "Sᴀʟᴛᴏ Iɴғɪɴɪᴛᴏ",
    flag = "InfJump",
    value = false,
    callback = function(value)
        if value then
            infJumpConn = UserInputService.JumpRequest:Connect(function()
                if LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Humanoid") then
                    LocalPlayer.Character:FindFirstChildOfClass("Humanoid"):ChangeState("Jumping")
                end
            end)
        else
            if infJumpConn then infJumpConn:Disconnect() infJumpConn = nil end
        end
    end
})

movementTab:CreateToggle({
    name = "Nᴏᴄʟɪᴘ (Aᴛʀᴀᴠᴇsᴀʀ Pᴀʀᴇᴅᴇs)",
    flag = "Noclip",
    value = false,
    callback = function(value)
        if value then
            noclipConn = RunService.Stepped:Connect(function()
                if LocalPlayer.Character then
                    for _, part in pairs(LocalPlayer.Character:GetDescendants()) do
                        if part:IsA("BasePart") then part.CanCollide = false end
                    end
                end
            end)
        else
            if noclipConn then noclipConn:Disconnect() noclipConn = nil end
        end
    end
})

-- ==========================================
-- 📍 6. TELEPORTS (SAN DIEGO)
-- ==========================================
local function teleportTo(cframe)
    local char = LocalPlayer.Character
    if char and char:FindFirstChild("HumanoidRootPart") then
        local veh = getMyVehicle()
        if veh then
            veh:SetPrimaryPartCFrame(cframe)
        else
            char.HumanoidRootPart.CFrame = cframe
        end
    end
end

tpTab:CreateButton({
    name = "Tᴇʟᴇᴘᴏʀᴛ: Fʀᴏɴᴛᴇʀᴀ Mᴇ́xɪᴄᴏ",
    callback = function() teleportTo(CFrame.new(150, 10, -500)) end
})

tpTab:CreateButton({
    name = "Tᴇʟᴇᴘᴏʀᴛ: Fʀᴏɴᴛᴇʀᴀ USA",
    callback = function() teleportTo(CFrame.new(150, 10, 500)) end
})

tpTab:CreateButton({
    name = "Tᴇʟᴇᴘᴏʀᴛ: Tɪᴇɴᴅᴀ ᴅᴇ Aʀᴍᴀs",
    callback = function() teleportTo(CFrame.new(-300, 10, 100)) end
})

-- ==========================================
-- 👁️ 7. VISIÓN
-- ==========================================
espTab:CreateToggle({
    name = "ESP Jᴜɢᴀᴅᴏʀᴇs (Cʜᴀᴍs)",
    flag = "PlayerESP",
    value = false,
    callback = function(value)
        for _, player in pairs(Players:GetPlayers()) do
            if player ~= LocalPlayer and player.Character then
                local hl = player.Character:FindFirstChild("UrielHighlight")
                if value then
                    if not hl then
                        hl = Instance.new("Highlight")
                        hl.Name = "UrielHighlight"
                        hl.FillColor = Color3.fromRGB(0, 255, 200)
                        hl.OutlineColor = Color3.fromRGB(255, 255, 255)
                        hl.Parent = player.Character
                    end
                else
                    if hl then hl:Destroy() end
                end
            end
        end
    end
})

espTab:CreateToggle({
    name = "Fᴜʟʟʙʀɪɢʜᴛ",
    flag = "Fullbright",
    value = false,
    callback = function(value)
        if value then
            Lighting.Brightness = 3
            Lighting.ClockTime = 12
            Lighting.GlobalShadows = false
        else
            Lighting.Brightness = 1
            Lighting.GlobalShadows = true
        end
    end
})

-- ==========================================
-- ⚡ 8. AFK / RENDIMIENTO
-- ==========================================
afkTab:CreateToggle({
    name = "Dᴇsᴀᴄᴛɪᴠᴀʀ Rᴇɴᴅᴇʀɪᴢᴀᴅᴏ 3D",
    flag = "Disable3D",
    value = false,
    callback = function(value) RunService:Set3dRenderingEnabled(not value) end
})

afkTab:CreateToggle({
    name = "Aɴᴛɪ-AFK (24/7)",
    flag = "AntiAFK",
    value = false,
    callback = function(value)
        if value then
            antiAfkConn = LocalPlayer.Idled:Connect(function()
                VirtualUser:CaptureController()
                VirtualUser:ClickButton2(Vector2.new())
            end)
        else
            if antiAfkConn then antiAfkConn:Disconnect() antiAfkConn = nil end
        end
    end
})

afkTab:CreateButton({
    name = "CPU Bᴏᴏsᴛᴇʀ (Mᴀ́xɪᴍᴏs FPS)",
    callback = function()
        for _, obj in pairs(game:GetDescendants()) do
            if obj:IsA("BasePart") then
                obj.Material = Enum.Material.SmoothPlastic
            elseif obj:IsA("Texture") or obj:IsA("Decal") then
                obj:Destroy()
            end
        end
    end
})

window:Notify({
    title = "Uʀɪᴇʟ Sᴛᴜᴅɪᴏ",
    content = "Mᴜʟᴛɪ-Gᴀᴍᴇ Uɴɪғɪᴄᴀᴅᴏ Vᴇʀsɪᴏ́ɴ Cᴏᴍᴘʟᴇᴛᴀ",
    duration = 3
})
