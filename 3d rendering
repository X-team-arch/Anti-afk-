local Rayfield = loadstring(game:HttpGet("https://sirius.menu/rayfield"))()

local RunService = game:GetService("RunService")
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local VirtualUser = game:GetService("VirtualUser")
local UserInputService = game:GetService("UserInputService")
local Lighting = game:GetService("Lighting")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Workspace = game:GetService("Workspace")

-- ==========================================
-- 🛡️ PROTECCIÓN ANTI-KICK & HOOKS
-- ==========================================
local rawget = rawget
local setreadonly = setreadonly or make_writeable
local getrawmetatable = getrawmetatable

if getrawmetatable and setreadonly then
    local gmt = getrawmetatable(game)
    setreadonly(gmt, false)
    local oldNamecall = gmt.__namecall

    gmt.__namecall = newcclosure(function(self, ...)
        local method = getnamecallmethod()
        if method == "Kick" or method == "kick" then
            Rayfield:Notify({Title = "Anti-Kick", Content = "Se bloqueó un intento de Kick del servidor.", Duration = 3})
            return nil
        end
        return oldNamecall(self, ...)
    end)
    setreadonly(gmt, true)
end

-- ==========================================
-- Ventana Principal
-- ==========================================
local Window = Rayfield:CreateWindow({
   Name = "⚡ URIEL STUDIO — Ultimate Edition",
   Icon = 0,
   LoadingTitle = "UrielAfk v3.0",
   LoadingSubtitle = "by Uriel — All-In-One",
   Theme = "Ocean",
   DisableRayfieldPrompts = false,
   DisableBuildWarnings = false,
   ConfigurationSaving = {
      Enabled = true,
      FolderName = "UrielStudioConfigs",
      FileName = "UrielUltimate_Config"
   },
   Discord = { Enabled = false },
   KeySystem = false
})

-- Pestañas
local SanDiegoTab = Window:CreateTab("🇲🇽 San Diego Border", 4483362458)
local CombatTab = Window:CreateTab("🎯 Combate & Armas", 4483362458)
local CarAdvTab = Window:CreateTab("🏎️ Vehículos Pro", 4483362458)
local MovementTab = Window:CreateTab("🏃 Movimiento", 4483362458)
local ESPTab = Window:CreateTab("👁️ Visión & ESP", 4483362458)
local AFKTab = Window:CreateTab("⚡ AFK & GPU", 4483362458)
local UtilitiesTab = Window:CreateTab("🛠️ Utilidades", 4483362458)

-- Variables de Estado
local altHeld = false
local antiCollisionConn, borderLimitConn, boatAvoidanceConn, motorbikeConn, altSpeedConn
local noclipConn, infJumpConn, flyConn, antiAfkConn, carFlyConn, noRecoilConn, aimbotConn
local flying = false
local carFlying = false
local flySpeed = 50
local carFlySpeed = 80

local sdSettings = {
    vehicleAltSpeed = 220,
    boatAltSpeed = 180,
    antiPit = false,
    boatAvoidance = false,
    motorbikeStabilizer = false
}

-- Función Auxiliar: Obtener Vehículo
local function getMyVehicle()
    local char = LocalPlayer.Character
    if char and char:FindFirstChildOfClass("Humanoid") then
        local seat = char:FindFirstChildOfClass("Humanoid").SeatPart
        if seat and seat.Parent then
            return seat.Parent, seat
        end
    end
    return nil, nil
end

-- Detectar Tecla ALT
UserInputService.InputBegan:Connect(function(input, gpe)
    if not gpe and (input.KeyCode == Enum.KeyCode.LeftAlt or input.KeyCode == Enum.KeyCode.RightAlt) then
        altHeld = true
    end
end)

UserInputService.InputEnded:Connect(function(input)
    if input.KeyCode == Enum.KeyCode.LeftAlt or input.KeyCode == Enum.KeyCode.RightAlt then
        altHeld = false
    end
end)

-- ==========================================
-- 🇲🇽 1. SAN DIEGO BORDER
-- ==========================================

SanDiegoTab:CreateSection("🏎️ Hacks de Vehículos & Frontera")

SanDiegoTab:CreateToggle({
   Name = "Anticolisión de Vehículos",
   CurrentValue = false,
   Flag = "SD_AntiCollision",
   Callback = function(Value)
        if Value then
            antiCollisionConn = RunService.Stepped:Connect(function()
                local veh = getMyVehicle()
                if veh then
                    for _, part in pairs(veh:GetDescendants()) do
                        if part:IsA("BasePart") then part.CanCollide = false end
                    end
                end
            end)
        else
            if antiCollisionConn then antiCollisionConn:Disconnect() antiCollisionConn = nil end
        end
   end,
})

SanDiegoTab:CreateToggle({
   Name = "Sin Límite de Velocidad en Frontera",
   CurrentValue = false,
   Flag = "SD_NoBorderLimit",
   Callback = function(Value)
        if Value then
            borderLimitConn = RunService.Heartbeat:Connect(function()
                for _, obj in pairs(Workspace:GetDescendants()) do
                    if obj.Name:lower():find("border") or obj.Name:lower():find("checkpoint") or obj.Name:lower():find("speedlimit") then
                        if obj:IsA("BasePart") then obj.CanTouch = false end
                    end
                end
            end)
        else
            if borderLimitConn then borderLimitConn:Disconnect() borderLimitConn = nil end
        end
   end,
})

SanDiegoTab:CreateToggle({
   Name = "Anti PIT (Protección de Choques)",
   CurrentValue = false,
   Flag = "SD_AntiPIT",
   Callback = function(Value)
        sdSettings.antiPit = Value
        if Value then
            RunService.Heartbeat:Connect(function()
                local veh, seat = getMyVehicle()
                if veh and sdSettings.antiPit then
                    local root = veh.PrimaryPart or seat
                    if root then root.AssemblyAngularVelocity = Vector3.new(0, root.AssemblyAngularVelocity.Y, 0) end
                end
            end)
        end
   end,
})

SanDiegoTab:CreateSlider({
   Name = "Velocidad de Auto con ALT",
   Range = {50, 500},
   Increment = 10,
   Suffix = " Speed",
   CurrentValue = sdSettings.vehicleAltSpeed,
   Flag = "SD_VehicleSpeed",
   Callback = function(Value) sdSettings.vehicleAltSpeed = Value end,
})

SanDiegoTab:CreateSlider({
   Name = "Velocidad de Lancha con ALT",
   Range = {50, 500},
   Increment = 10,
   Suffix = " Speed",
   CurrentValue = sdSettings.boatAltSpeed,
   Flag = "SD_BoatSpeed",
   Callback = function(Value) sdSettings.boatAltSpeed = Value end,
})

altSpeedConn = RunService.Heartbeat:Connect(function()
    if altHeld then
        local veh, seat = getMyVehicle()
        if veh and seat then
            local root = veh.PrimaryPart or seat
            local isBoat = veh.Name:lower():find("boat") or veh.Name:lower():find("lancha") or seat.Name:lower():find("boat")
            local speed = isBoat and sdSettings.boatAltSpeed or sdSettings.vehicleAltSpeed
            root.AssemblyLinearVelocity = root.CFrame.LookVector * speed
        end
    end
end)

SanDiegoTab:CreateSection("🛥️ Náutica & Motos")

SanDiegoTab:CreateToggle({
   Name = "Evasión de Obstáculos (Barco Auto-Pilot)",
   CurrentValue = false,
   Flag = "SD_BoatAvoid",
   Callback = function(Value)
        sdSettings.boatAvoidance = Value
        if Value then
            boatAvoidanceConn = RunService.Heartbeat:Connect(function()
                local veh, seat = getMyVehicle()
                if veh and seat then
                    local root = veh.PrimaryPart or seat
                    local rayParams = RaycastParams.new()
                    rayParams.FilterDescendantsInstances = {veh, LocalPlayer.Character}
                    rayParams.FilterType = Enum.RaycastFilterType.Exclude

                    local result = Workspace:Raycast(root.Position, root.CFrame.LookVector * 30, rayParams)
                    if result and result.Instance.CanCollide then
                        root.AssemblyAngularVelocity = Vector3.new(0, 4, 0)
                    end
                end
            end)
        else
            if boatAvoidanceConn then boatAvoidanceConn:Disconnect() boatAvoidanceConn = nil end
        end
   end,
})

SanDiegoTab:CreateToggle({
   Name = "Estabilizador de Moto",
   CurrentValue = false,
   Flag = "SD_MotoStab",
   Callback = function(Value)
        sdSettings.motorbikeStabilizer = Value
        if Value then
            motorbikeConn = RunService.RenderStepped:Connect(function()
                local veh, seat = getMyVehicle()
                if veh and seat then
                    local root = veh.PrimaryPart or seat
                    local rx, ry, rz = root.CFrame:ToOrientation()
                    root.CFrame = CFrame.new(root.Position) * CFrame.Angles(0, ry, 0)
                end
            end)
        else
            if motorbikeConn then motorbikeConn:Disconnect() motorbikeConn = nil end
        end
   end,
})

SanDiegoTab:CreateButton({
   Name = "Enderezar / Voltear Vehículo",
   Callback = function()
        local veh, seat = getMyVehicle()
        if veh and seat then
            local root = veh.PrimaryPart or seat
            root.CFrame = CFrame.new(root.Position + Vector3.new(0, 4, 0)) * CFrame.Angles(0, math.rad(root.Orientation.Y), 0)
            Rayfield:Notify({Title = "San Diego", Content = "Vehículo enderezado.", Duration = 2})
        end
   end,
})

-- ==========================================
-- 🎯 2. COMBATE & ARMAS
-- ==========================================

CombatTab:CreateToggle({
   Name = "Sin Retroceso (No Recoil / Spread)",
   CurrentValue = false,
   Flag = "Combat_NoRecoil",
   Callback = function(Value)
        if Value then
            noRecoilConn = RunService.Stepped:Connect(function()
                local char = LocalPlayer.Character
                if char then
                    local tool = char:FindFirstChildOfClass("Tool")
                    if tool then
                        for _, v in pairs(tool:GetDescendants()) do
                            if v:IsA("Value") and (v.Name:find("Recoil") or v.Name:find("Spread")) then
                                v.Value = 0
                            end
                        end
                    end
                end
            end)
        else
            if noRecoilConn then noRecoilConn:Disconnect() noRecoilConn = nil end
        end
   end,
})

-- ==========================================
-- 🏎️ 3. VEHÍCULOS PRO
-- ==========================================

CarAdvTab:CreateToggle({
   Name = "Vuelo de Auto (Car Fly)",
   CurrentValue = false,
   Flag = "Car_Fly",
   Callback = function(Value)
        carFlying = Value
        if carFlying then
            carFlyConn = RunService.RenderStepped:Connect(function()
                local veh, seat = getMyVehicle()
                if veh and seat then
                    local root = veh.PrimaryPart or seat
                    root.AssemblyLinearVelocity = Workspace.CurrentCamera.CFrame.LookVector * carFlySpeed
                end
            end)
        else
            if carFlyConn then carFlyConn:Disconnect() carFlyConn = nil end
        end
   end,
})

CarAdvTab:CreateSlider({
   Name = "Velocidad de Vuelo de Auto",
   Range = {30, 300},
   Increment = 10,
   CurrentValue = carFlySpeed,
   Flag = "Car_FlySpeed",
   Callback = function(Value) carFlySpeed = Value end,
})

CarAdvTab:CreateButton({
   Name = "Freno Instantáneo",
   Callback = function()
        local veh, seat = getMyVehicle()
        if veh and seat then
            local root = veh.PrimaryPart or seat
            root.AssemblyLinearVelocity = Vector3.zero
            root.AssemblyAngularVelocity = Vector3.zero
        end
   end,
})

-- ==========================================
-- 🏃 4. MOVIMIENTO
-- ==========================================

MovementTab:CreateSlider({
   Name = "Velocidad de Caminado",
   Range = {16, 300},
   Increment = 1,
   Suffix = " WalkSpeed",
   CurrentValue = 16,
   Flag = "WalkSpeed",
   Callback = function(Value)
        if LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Humanoid") then
            LocalPlayer.Character:FindFirstChildOfClass("Humanoid").WalkSpeed = Value
        end
   end,
})

MovementTab:CreateToggle({
   Name = "Salto Infinito",
   CurrentValue = false,
   Flag = "InfJump",
   Callback = function(Value)
        if Value then
            infJumpConn = UserInputService.JumpRequest:Connect(function()
                if LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Humanoid") then
                    LocalPlayer.Character:FindFirstChildOfClass("Humanoid"):ChangeState("Jumping")
                end
            end)
        else
            if infJumpConn then infJumpConn:Disconnect() infJumpConn = nil end
        end
   end,
})

MovementTab:CreateToggle({
   Name = "Noclip",
   CurrentValue = false,
   Flag = "Noclip",
   Callback = function(Value)
        if Value then
            noclipConn = RunService.Stepped:Connect(function()
                if LocalPlayer.Character then
                    for _, part in pairs(LocalPlayer.Character:GetDescendants()) do
                        if part:IsA("BasePart") then part.CanCollide = false end
                    end
                end
            end)
        else
            if noclipConn then noclipConn:Disconnect() noclipConn = nil end
        end
   end,
})

MovementTab:CreateToggle({
   Name = "Modo Vuelo (Fly)",
   CurrentValue = false,
   Flag = "Fly",
   Callback = function(Value)
        flying = Value
        local char = LocalPlayer.Character
        if not char or not char:FindFirstChild("HumanoidRootPart") then return end
        local hrp = char.HumanoidRootPart

        if flying then
            local bv = Instance.new("BodyVelocity")
            bv.Name = "UrielFly"
            bv.MaxForce = Vector3.new(1e9, 1e9, 1e9)
            bv.Parent = hrp

            flyConn = RunService.RenderStepped:Connect(function()
                if flying and hrp:FindFirstChild("UrielFly") then
                    bv.Velocity = Workspace.CurrentCamera.CFrame.LookVector * flySpeed
                end
            end)
        else
            if hrp:FindFirstChild("UrielFly") then hrp.UrielFly:Destroy() end
            if flyConn then flyConn:Disconnect() flyConn = nil end
        end
   end,
})

-- ==========================================
-- 👁️ 5. VISIÓN & ESP
-- ==========================================

ESPTab:CreateToggle({
   Name = "ESP Jugadores (Chams)",
   CurrentValue = false,
   Flag = "PlayerESP",
   Callback = function(Value)
        for _, player in pairs(Players:GetPlayers()) do
            if player ~= LocalPlayer and player.Character then
                local hl = player.Character:FindFirstChild("UrielHighlight")
                if Value then
                    if not hl then
                        hl = Instance.new("Highlight")
                        hl.Name = "UrielHighlight"
                        hl.FillColor = Color3.fromRGB(0, 255, 200)
                        hl.OutlineColor = Color3.fromRGB(255, 255, 255)
                        hl.Parent = player.Character
                    end
                else
                    if hl then hl:Destroy() end
                end
            end
        end
   end,
})

ESPTab:CreateToggle({
   Name = "Fullbright (Visión Nocturna)",
   CurrentValue = false,
   Flag = "Fullbright",
   Callback = function(Value)
        if Value then
            Lighting.Brightness = 3
            Lighting.ClockTime = 12
            Lighting.GlobalShadows = false
        else
            Lighting.Brightness = 1
            Lighting.GlobalShadows = true
        end
   end,
})

-- ==========================================
-- ⚡ 6. AFK & RENDIMIENTO
-- ==========================================

AFKTab:CreateToggle({
   Name = "Desactivar Renderizado 3D (Ahorro GPU)",
   CurrentValue = false,
   Flag = "Disable3D",
   Callback = function(Value) RunService:Set3dRenderingEnabled(not Value) end,
})

AFKTab:CreateToggle({
   Name = "Anti-AFK (24/7)",
   CurrentValue = false,
   Flag = "AntiAFK",
   Callback = function(Value)
        if Value then
            antiAfkConn = LocalPlayer.Idled:Connect(function()
                VirtualUser:CaptureController()
                VirtualUser:ClickButton2(Vector2.new())
            end)
        else
            if antiAfkConn then antiAfkConn:Disconnect() antiAfkConn = nil end
        end
   end,
})

AFKTab:CreateButton({
   Name = "CPU Booster (Remover Texturas)",
   Callback = function()
        for _, obj in pairs(game:GetDescendants()) do
            if obj:IsA("BasePart") then
                obj.Material = Enum.Material.SmoothPlastic
            elseif obj:IsA("Texture") or obj:IsA("Decal") then
                obj:Destroy()
            end
        end
   end,
})

-- ==========================================
-- 🛠️ 7. UTILIDADES
-- ==========================================

UtilitiesTab:CreateButton({
   Name = "Server Hop (Cambiar Servidor)",
   Callback = function()
        local Http = game:GetService("HttpService")
        local TPS = game:GetService("TeleportService")
        local Api = "https://games.roblox.com/v1/games/" .. game.PlaceId .. "/servers/Public?sortOrder=Asc&limit=100"
        local success, result = pcall(function() return Http:JSONDecode(game:HttpGet(Api)) end)
        if success and result and result.data then
            for _, server in pairs(result.data) do
                if server.playing < server.maxPlayers and server.id ~= game.JobId then
                    TPS:TeleportToPlaceInstance(game.PlaceId, server.id, LocalPlayer)
                    break
                end
            end
        end
   end,
})

UtilitiesTab:CreateButton({
   Name = "Rejoin (Reconectar)",
   Callback = function()
        game:GetService("TeleportService"):Teleport(game.PlaceId, LocalPlayer)
   end,
})

UtilitiesTab:CreateKeybind({
   Name = "Ocultar/Mostrar Menú",
   CurrentKeybind = "K",
   HoldToInteract = false,
   Flag = "MenuKeybind",
   Callback = function() end,
})
